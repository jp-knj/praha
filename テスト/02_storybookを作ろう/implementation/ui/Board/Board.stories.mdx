import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { Board } from "./Board";

<Meta title="MDX/Board" component={Board} />

# Board
With `MDX`, we can define a story for `Board` right in the middle of our Markdown documentation.

<Canvas>
  <Story name="board">
    <Board
      winner='X'
      player='X'
      boards={Array(9).fill("△")}
      handlePlayClick={() => null}
      renderStatus={() => <></>}
    />
  </Story>
</Canvas>
